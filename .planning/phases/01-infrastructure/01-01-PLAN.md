---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - index.html
  - src/main.ts
  - src/game/main.ts
  - src/game/scenes/Boot.ts
  - public/style.css
autonomous: true
requirements: [FOUND-01, FOUND-03]

must_haves:
  truths:
    - "`npm run dev` starts a local dev server on localhost:5173 with no console errors"
    - "`npm run build` produces a `dist/` directory with no TypeScript or Vite errors"
    - "A black canvas renders in the browser — no white/blank page, no JS exceptions"
    - "Canvas pixels are crisp at 100% browser zoom — no anti-aliasing blur on pixel edges"
    - "Grid Engine plugin is registered in the Phaser config (ready for Phase 2 use)"
  artifacts:
    - path: "package.json"
      provides: "npm scripts (dev, build, preview) and dependency declarations"
      contains: "phaser, grid-engine, vite, typescript"
    - path: "vite.config.ts"
      provides: "Vite configuration for Phaser + TypeScript"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "index.html"
      provides: "HTML entry point mounting the Phaser canvas"
    - path: "src/main.ts"
      provides: "App entry — imports and starts the Phaser game"
    - path: "src/game/main.ts"
      provides: "Phaser game config with pixelArt:true and Grid Engine plugin"
    - path: "src/game/scenes/Boot.ts"
      provides: "Minimal Boot scene (blank canvas smoke test)"
    - path: "public/style.css"
      provides: "canvas image-rendering: pixelated CSS"
  key_links:
    - from: "src/main.ts"
      to: "src/game/main.ts"
      via: "import"
      pattern: "import.*game/main"
    - from: "src/game/main.ts"
      to: "src/game/scenes/Boot.ts"
      via: "scene array in Phaser.Game config"
      pattern: "scene.*BootScene"
    - from: "index.html"
      to: "src/main.ts"
      via: "script type=module src"
      pattern: "src/main.ts"
    - from: "index.html"
      to: "public/style.css"
      via: "link rel=stylesheet"
      pattern: "style.css"
---

<objective>
Bootstrap the Phaser 3 + Grid Engine + Vite + TypeScript project scaffold from scratch. The deliverable is a blank canvas that boots locally and produces a clean production build — no gameplay, just the skeleton every later phase builds on.

Purpose: All 9 phases depend on this scaffold existing. Nothing can be built until `npm run dev` works.
Output: A complete project structure with all config files, a minimal Boot scene, and pixel-perfect rendering configured.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Key patterns confirmed from research. Use these exactly — do not deviate. -->

<!-- package.json scripts -->
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
```

<!-- Phaser game config — src/game/main.ts -->
```typescript
import Phaser from "phaser";
import { GridEngine } from "grid-engine";
import { BootScene } from "./scenes/Boot";

const config: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  pixelArt: true,           // Sets antialias:false + roundPixels:true
  backgroundColor: "#1a1a2e",
  plugins: {
    scene: [
      {
        key: "GridEngine",
        plugin: GridEngine,
        mapping: "gridEngine",
      },
    ],
  },
  scene: [BootScene],
};

export default new Phaser.Game(config);
```

<!-- CSS — public/style.css -->
```css
* { margin: 0; padding: 0; box-sizing: border-box; }
body { background: #000; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
canvas {
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  -ms-interpolation-mode: nearest-neighbor;
}
```

<!-- Grid Engine TypeScript augmentation — src/types/global.d.ts -->
```typescript
import { GridEngine } from "grid-engine";
declare module "phaser" {
  interface Scene {
    gridEngine: GridEngine;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap project scaffold (package.json, vite config, tsconfig, index.html)</name>
  <files>package.json, vite.config.ts, tsconfig.json, index.html</files>
  <action>
Create the four root-level configuration files for the Phaser 3 + Vite + TypeScript project.

**package.json** — name "andres-world", version "0.1.0", private:true. Scripts: `"dev": "vite"`, `"build": "tsc && vite build"`, `"preview": "vite preview"`. Dependencies: `"phaser": "^3.90.0"`, `"grid-engine": "^2.48.0"`. DevDependencies: `"vite": "^6.0.0"`, `"typescript": "^5.7.0"`, `"@types/node": "latest"`. After writing package.json, run `npm install` to install all dependencies.

**vite.config.ts** — minimal Vite config. Set `base: "./"` so asset paths work correctly in the Vercel-deployed build. No other changes needed — Vite auto-detects TypeScript.

```typescript
import { defineConfig } from "vite";

export default defineConfig({
  base: "./",
});
```

**tsconfig.json** — standard strict TypeScript config for browser Vite projects:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "outDir": "./dist"
  },
  "include": ["src"]
}
```

**index.html** — bare HTML5 entry point. Link to `style.css` and load `src/main.ts` as a module:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Andres World</title>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

Note: `style.css` lives in `public/` (served at root), but index.html links it as `./style.css` — this resolves correctly in both dev and production.
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npm run build</automated>
  </verify>
  <done>package.json exists with correct scripts and deps. `npm install` succeeded (node_modules present). `npm run build` exits 0 and produces a `dist/` directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phaser source files (game config, Boot scene, entry point, CSS, global types)</name>
  <files>src/main.ts, src/game/main.ts, src/game/scenes/Boot.ts, src/types/global.d.ts, public/style.css</files>
  <action>
Create the source files. All assets belong in `public/` (not `src/assets/`) so Vite doesn't hash them — this is critical for Phaser's loader in later phases.

**src/main.ts** — application entry point. Import and start the game:

```typescript
import "./game/main";
```

That's it. The game config in `src/game/main.ts` creates and exports the `Phaser.Game` instance on import.

**src/game/main.ts** — Phaser game config. Use the exact config from the interfaces block above. `pixelArt: true` is the single flag that enables `antialias: false` + `roundPixels: true`. Grid Engine registered as a scene plugin with mapping "gridEngine" so `this.gridEngine` is accessible from scenes in Phase 2+.

**src/game/scenes/Boot.ts** — minimal boot scene:

```typescript
import Phaser from "phaser";

export class BootScene extends Phaser.Scene {
  constructor() {
    super({ key: "Boot" });
  }

  create(): void {
    // Phase 1: blank canvas confirms scaffold is working
    // Phase 2: this scene will load the tilemap and transition to the Overworld scene
    console.log("Andres World — scaffold running");
  }
}
```

**src/types/global.d.ts** — TypeScript module augmentation for Grid Engine plugin mapping. Prevents red squiggles on `this.gridEngine` throughout the codebase in Phase 2+:

```typescript
import { GridEngine } from "grid-engine";

declare module "phaser" {
  interface Scene {
    gridEngine: GridEngine;
  }
}
```

**public/style.css** — canvas pixel rendering CSS. Use the exact rules from the interfaces block. This prevents browser CSS scaling from re-introducing smoothing after Phaser's `pixelArt: true` config.

Do NOT put assets in `src/assets/` — Vite will hash them and break Phaser's string-based loader. `public/assets/` is the correct location (already established from CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>All 5 files exist. `npm run build` exits 0. `tsc --noEmit` exits 0 with no type errors. `dist/` contains bundled output.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` exits 0 — TypeScript compiles, Vite bundles, `dist/` produced
2. `npx tsc --noEmit` exits 0 — no type errors anywhere in `src/`
3. `npm run dev` starts a server on localhost:5173 — browser opens to a black canvas (800x600, no white page, no console errors)
4. Visual check at 100% zoom: canvas border is a crisp pixel edge (no blur)
5. Grid Engine import does not throw at runtime (check console — "Andres World — scaffold running" log appears)
</verification>

<success_criteria>
- `npm run build` exits 0, `dist/` exists
- `npx tsc --noEmit` exits 0
- Browser at localhost:5173 shows a dark canvas (not a white/blank error page)
- No console errors in browser dev tools
- "Andres World — scaffold running" appears in browser console
- Canvas at 100% zoom: pixel-crisp edges (FOUND-03 satisfied visually)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md` with:
- Files created
- npm script verification results
- Any deviations from the plan (e.g., version changes)
- Confirmation that pixel-perfect rendering is configured
</output>
