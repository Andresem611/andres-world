---
phase: 03.4-character-npc-sprites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/sprites/player.png
  - public/assets/sprites/npc-marc-andreessen.png
  - public/assets/sprites/npc-paul-graham.png
  - public/assets/sprites/npc-michael-seibel.png
  - public/assets/sprites/npc-keri.png
  - public/assets/sprites/npc-dad.png
  - src/game/scenes/Boot.ts
  - .planning/SPRITE-SOURCES.md
autonomous: false
requirements:
  - CHAR-04
  - CHAR-05

must_haves:
  truths:
    - "Player character renders as hoodie+backpack sprite with 4-directional walk animation"
    - "At least 5 NPC sprites show real pixel art instead of colored-box placeholders"
    - "All sprite sources and licenses documented in SPRITE-SOURCES.md"
    - "Spritesheet matches PIPOYA row order (Down/Left/Right/Up) so Grid Engine walkingAnimationMapping works"
  artifacts:
    - path: "public/assets/sprites/player.png"
      provides: "Player character spritesheet (32x32 frames, 4 directions)"
      contains: "PNG file > 1KB"
    - path: ".planning/SPRITE-SOURCES.md"
      provides: "Sprite source attribution and license documentation"
      min_lines: 10
  key_links:
    - from: "src/game/scenes/Boot.ts"
      to: "public/assets/sprites/player.png"
      via: "this.load.spritesheet('player', 'assets/sprites/player.png')"
      pattern: "load\\.spritesheet.*player"
---

<objective>
Replace the placeholder player sprite and at least 5 NPC sprites with real pixel art generated via AI (PixelLab). The player gets a 4-directional walk cycle spritesheet; NPCs get static front-facing 32x32 sprites.

Purpose: CHAR-04 and CHAR-05 require real pixel art. The placeholder colored blocks break immersion.
Output: Real player spritesheet + 5+ NPC sprite PNGs + SPRITE-SOURCES.md documentation.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-character-npc-sprites/03.4-RESEARCH.md

@src/game/scenes/Boot.ts

<interfaces>
<!-- Current player sprite loading in Boot.ts -->
```typescript
// Boot.ts line 31-34
this.load.spritesheet("player", "assets/sprites/character-placeholder.png", {
  frameWidth: 32,
  frameHeight: 32,
});

// NPC loading pattern (single images, not spritesheets)
this.load.image(`npc-${id}`, `assets/sprites/npc-${id}.png`);
```

<!-- PIPOYA row order (CRITICAL - must match) -->
```
Row 0: Down  (frames 0, 1, 2 or 0, 1, 2, 3)
Row 1: Left  (frames 3, 4, 5 or 4, 5, 6, 7)
Row 2: Right (frames 6, 7, 8 or 8, 9, 10, 11)
Row 3: Up    (frames 9, 10, 11 or 12, 13, 14, 15)
```

<!-- Grid Engine config in Overworld.ts -->
```typescript
walkingAnimationMapping: 0  // Must stay 0. Spritesheet layout conforms to engine.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate sprites in PixelLab + post-process in Aseprite</name>
  <files>public/assets/sprites/player.png, public/assets/sprites/npc-marc-andreessen.png, public/assets/sprites/npc-paul-graham.png, public/assets/sprites/npc-michael-seibel.png, public/assets/sprites/npc-keri.png, public/assets/sprites/npc-dad.png</files>
  <action>
    Generate pixel art sprites using PixelLab ($9/mo) and drop them into the project.

    **Player character (CHAR-04):**
    1. Open PixelLab Character Creator
    2. Prompt: "top-down RPG character, hoodie, laptop backpack, 32x32, pixel art, 4-directional walk cycle"
    3. Download the spritesheet PNG
    4. Open in Aseprite:
       - Verify frame order is PIPOYA: Down/Left/Right/Up
       - If not PIPOYA order, reorder rows to match: Row 0=Down, Row 1=Left, Row 2=Right, Row 3=Up
       - Flatten to indexed color mode (max 15 colors + transparent) to remove anti-aliasing halos
       - If 4 frames per direction: export as 128x128px PNG (4 cols x 4 rows x 32px)
       - If 3 frames per direction: export as 96x128px PNG (3 cols x 4 rows x 32px)
    5. Save as: public/assets/sprites/player.png

    **NPC sprites (CHAR-05) -- at least 5, ideally all 14:**
    Priority NPCs (most visible on main paths):
    - Marc Andreessen, Paul Graham, Michael Seibel, Keri, Dad
    Secondary: John Collison, Brian Chesky, Dalton Caldwell, Ben Horowitz, Vinod Khosla, Tobi Lutke, Patrick Collison, Dario Amodei, Dog-1

    For each NPC:
    1. Generate a single front-facing 32x32 pixel art character in PixelLab
    2. Use the same session/style settings for visual consistency
    3. Save as 32x32 PNG with transparent background
    4. Replace the existing placeholder at: public/assets/sprites/npc-{id}.png

    **Important notes:**
    - All sprites must be 32x32 pixel frames
    - Transparent backgrounds (no colored block behind character)
    - Max 15 colors + transparent per sprite for pixel art crispness
    - NPCs are STATIC single-frame PNGs (not spritesheets) -- no animation needed
    - Player spritesheet row order MUST be Down/Left/Right/Up (PIPOYA) or Grid Engine walk animation will be scrambled
  </action>
  <verify>Type "sprites done" when player.png and at least 5 NPC PNGs are saved to public/assets/sprites/</verify>
  <done>player.png exists as a valid spritesheet (96x128 or 128x128). At least 5 npc-*.png files replaced with real pixel art PNGs > 200 bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Update Boot.ts + create SPRITE-SOURCES.md + verify integration</name>
  <files>src/game/scenes/Boot.ts, .planning/SPRITE-SOURCES.md</files>
  <action>
    1. Update src/game/scenes/Boot.ts:
       - Change the player spritesheet load path from "assets/sprites/character-placeholder.png" to "assets/sprites/player.png"
       - Keep frameWidth: 32 and frameHeight: 32 (unchanged)
       - NPC loading (this.load.image) does NOT need changes -- same file paths, just new PNG contents

    2. Create .planning/SPRITE-SOURCES.md documenting all sprite sources:
       ```markdown
       # Sprite Sources

       | Sprite | File | Source | License | Date |
       |--------|------|--------|---------|------|
       | Player (hoodie+backpack) | player.png | PixelLab AI generation | PixelLab ToS (commercial use OK) | 2026-03-14 |
       | Marc Andreessen | npc-marc-andreessen.png | PixelLab AI generation | PixelLab ToS | 2026-03-14 |
       | ... (one row per sprite) |
       ```
       List every sprite file that was replaced. Include which tool was used and the license terms.

    3. Verify the player spritesheet dimensions are valid:
       - Check PNG dimensions: should be either 96x128 (3 frames) or 128x128 (4 frames)
       - If 4 frames per direction (128x128), Grid Engine walkingAnimationMapping:0 auto-detects -- no code change needed
       - If dimensions are unexpected, note in summary for manual review

    4. Run `npx vite build` to verify no build errors.
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Boot.ts loads player.png (not character-placeholder.png). SPRITE-SOURCES.md documents all sprites. Build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of sprites in browser</name>
  <files>N/A</files>
  <action>
    Human verifies the real sprites render correctly in the running game.

    Steps:
    1. Run `npx vite dev` in the andres-world project
    2. Open http://localhost:5173 in browser
    3. Verify player character:
       - Shows hoodie + backpack (not colored block placeholder)
       - Walk in all 4 directions -- animation plays correctly (down/left/right/up match actual key presses)
       - If walk directions are scrambled (e.g., pressing down plays left animation), the PIPOYA row order is wrong -- needs Aseprite fix
    4. Walk to at least 5 NPCs and verify they show real pixel art sprites (not colored boxes)
    5. Check that NPC sprites have transparent backgrounds (no colored block behind them)
  </action>
  <verify>Type "approved" if sprites look correct, or describe any issues (scrambled directions, wrong colors, etc.)</verify>
  <done>Player walk animation matches key presses in all 4 directions. At least 5 NPCs show real pixel art. No visual artifacts.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors
- `ls -la public/assets/sprites/player.png` shows file exists and is > 1KB
- At least 5 npc-*.png files in public/assets/sprites/ are > 200 bytes (not tiny placeholder stubs)
- .planning/SPRITE-SOURCES.md exists with sprite attribution table
- Human confirms walk animation directions match key presses
</verification>

<success_criteria>
- Player character renders with hoodie+backpack sprite and smooth 4-directional walk animation
- At least 5 NPC sprites are real pixel art (not colored-box placeholders)
- PIPOYA row order confirmed correct (down/left/right/up match key presses)
- All sprite sources documented in SPRITE-SOURCES.md
- Build passes, no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-character-npc-sprites/03.4-02-SUMMARY.md`
</output>
