---
phase: 03.4-character-npc-sprites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/dialog.ts
  - src/content/dialogue.ts
  - src/game/config/npcs.ts
  - src/game/scenes/Overworld.ts
  - tests/dialogue.test.ts
  - tests/npc-config.test.ts
autonomous: true
requirements:
  - NPC-06

must_haves:
  truths:
    - "DialogEntry interface exists in src/types/dialog.ts with speaker, lines, condition, link, onComplete fields"
    - "All 14 NPC dialogue strings live in src/content/dialogue.ts keyed by NPC id"
    - "npcs.ts no longer contains inline dialog string arrays"
    - "Overworld.ts reads dialogue from DIALOGUE map, not from npc.dialog"
    - "All existing tests pass after extraction"
  artifacts:
    - path: "src/types/dialog.ts"
      provides: "DialogEntry and GameState interfaces"
      exports: ["DialogEntry", "GameState"]
    - path: "src/content/dialogue.ts"
      provides: "DIALOGUE record keyed by NPC id + sign/building content"
      exports: ["DIALOGUE"]
    - path: "tests/dialogue.test.ts"
      provides: "NPC-06 validation tests"
      min_lines: 20
  key_links:
    - from: "src/content/dialogue.ts"
      to: "src/types/dialog.ts"
      via: "import DialogEntry"
      pattern: "import.*DialogEntry.*from.*dialog"
    - from: "src/game/scenes/Overworld.ts"
      to: "src/content/dialogue.ts"
      via: "import DIALOGUE for interactionMap registration"
      pattern: "import.*DIALOGUE.*from.*content/dialogue"
---

<objective>
Extract all NPC dialogue strings from inline arrays in npcs.ts into a typed content layer (src/content/dialogue.ts + src/types/dialog.ts). This decouples NPC position data from content, enabling conditional dialogue and link interactions in Phase 4+.

Purpose: ADR Decision 2 requires DialogEntry interface before Phase 4. Inline string arrays collapse at Thoven HQ metrics board complexity.
Output: Typed dialogue system with all 14 NPC dialogues + sign/building text extracted and tested.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-character-npc-sprites/03.4-RESEARCH.md

@src/game/config/npcs.ts
@src/game/scenes/Overworld.ts
@tests/npc-config.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DialogEntry interface + dialogue content file + tests</name>
  <files>src/types/dialog.ts, src/content/dialogue.ts, tests/dialogue.test.ts</files>
  <behavior>
    - Test 1: DialogEntry interface has required fields (lines: string[]) and optional fields (speaker, condition, link, onComplete)
    - Test 2: DIALOGUE map has an entry for every NPC id in NPC_CONFIG (14 entries)
    - Test 3: Every DIALOGUE entry has a non-empty lines array
    - Test 4: DIALOGUE includes sign entries ("welcome-sign") and under-construction entries ("under-construction-default")
    - Test 5: No DIALOGUE entry has lines containing "TODO" or "[placeholder]"
  </behavior>
  <action>
    1. Create src/types/dialog.ts with DialogEntry and GameState interfaces exactly as defined in ADR Decision 2:
       - DialogEntry: speaker? string, lines: string[], condition? keyof GameState, link? { label: string; url: string }, onComplete? string
       - GameState: index signature [key: string]: boolean (placeholder for Phase 4+)

    2. Create src/content/dialogue.ts:
       - Import DialogEntry from ../types/dialog
       - Export DIALOGUE: Record<string, DialogEntry>
       - Extract all 14 NPC dialog arrays from npcs.ts into entries keyed by NPC id
       - Add sign entries: "welcome-sign" with lines ["Welcome to Andres World.", "Population: always building."]
       - Add under-construction entry: "under-construction-default" with lines ["Builder still hammering away... check back soon."]
       - Organize with zone comments (Main Street, Thoven HQ area, Beach, etc.) for readability

    3. Create tests/dialogue.test.ts with all 5 behavior tests above.
       - Import DIALOGUE from src/content/dialogue
       - Import NPC_CONFIG from src/game/config/npcs (to cross-reference ids)
       - Verify type correctness by importing DialogEntry

    Write tests first (RED), then implement dialog.ts and dialogue.ts (GREEN).
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npx vitest run tests/dialogue.test.ts</automated>
  </verify>
  <done>DialogEntry interface exported from src/types/dialog.ts. DIALOGUE map in src/content/dialogue.ts has entries for all 14 NPCs + signs + under-construction. All 5 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire npcs.ts and Overworld.ts to use content layer</name>
  <files>src/game/config/npcs.ts, src/game/scenes/Overworld.ts, tests/npc-config.test.ts</files>
  <action>
    1. Update src/game/config/npcs.ts:
       - Remove the `dialog: string[]` field from NpcDefinition interface
       - Add `dialogId: string` field to NpcDefinition (references key in DIALOGUE map)
       - Set each NPC's dialogId to their id value (e.g., dialogId: "marc-andreessen")
       - Remove inline dialog arrays from all 14 entries
       - PatrolNpcDefinition extends the updated NpcDefinition (no dialog field either)

    2. Update src/game/scenes/Overworld.ts:
       - Add import: `import { DIALOGUE } from "../../content/dialogue";`
       - In the interactionMap NPC registration loop (around line 122), change:
         `dialog: npc.dialog` to `dialog: DIALOGUE[npc.dialogId]?.lines ?? ["..."]`
       - For sign entries (line 135), change to: `dialog: DIALOGUE["welcome-sign"]?.lines ?? ["Welcome to Andres World."]`
         OR keep inline if sign text is simple enough -- use DIALOGUE for signs too for consistency.
       - For under-construction entries (lines 132-133), use DIALOGUE["under-construction-default"]

    3. Update tests/npc-config.test.ts:
       - Remove the "NPC-02: dialog array" test (dialog field no longer exists on NpcDefinition)
       - Remove the "NPC-03: no placeholder dialog" test (moved to dialogue.test.ts)
       - Add new test: "every entry has a non-empty dialogId string" that verifies dialogId exists and is a string
       - Keep NPC-01 (spriteKey), NPC-04 (patrol), NPC-05 (count=14) tests unchanged

    4. Run full test suite to confirm nothing broken.
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npx vitest run</automated>
  </verify>
  <done>npcs.ts has dialogId (not dialog) on all entries. Overworld.ts reads from DIALOGUE import. npc-config.test.ts updated. Full vitest suite green. No runtime regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests green (dialogue.test.ts + updated npc-config.test.ts + existing tests)
- `grep -r "dialog:" src/game/config/npcs.ts` returns only `dialogId:` lines (no inline string arrays)
- `grep "DIALOGUE" src/game/scenes/Overworld.ts` confirms import and usage
</verification>

<success_criteria>
- DialogEntry interface matches ADR Decision 2 spec exactly
- All 14 NPC dialogues extracted to src/content/dialogue.ts
- Sign and under-construction text also in DIALOGUE map
- npcs.ts contains zero inline dialogue strings
- Overworld.ts uses DIALOGUE import for all interaction text
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-character-npc-sprites/03.4-01-SUMMARY.md`
</output>
