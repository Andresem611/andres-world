---
phase: 02-overworld-map
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [WORLD-01, WORLD-02, WORLD-03, WORLD-04, WORLD-05, WORLD-06, WORLD-07, WORLD-08, CHAR-01, CHAR-02, CHAR-03]

must_haves:
  truths:
    - "Player character spawns at south dock facing north on page load"
    - "Arrow keys and WASD move the character one tile at a time in all 4 directions"
    - "Walking animation plays during movement; idle standing frame shows when stopped"
    - "Camera scrolls with the player; world edges clamp the camera"
    - "Buildings, water, and palm trees stop movement (collision works)"
    - "Map reads as Miami: palm trees on west/plaza, ocean on east, distinct dock entry from south"
    - "All 6 zones are visually distinguishable: dock, main street, plaza, west side, beach strip, heights"
    - "Under-construction buildings (Chalk Lab, VC Office) show scaffolding visuals"
  artifacts:
    - path: "public/assets/maps/overworld.json"
      provides: "Complete 50x40 Miami map"
    - path: "src/game/scenes/Overworld.ts"
      provides: "Playable overworld scene"
    - path: "public/assets/sprites/character-placeholder.png"
      provides: "4-directional character sprite"
  key_links:
    - from: "BootScene.preload()"
      to: "public/assets/maps/overworld.json"
      via: "this.load.tilemapTiledJSON('overworld', 'assets/maps/overworld.json')"
      pattern: "tilemapTiledJSON"
    - from: "OverworldScene tileset name"
      to: "overworld.json tilesets[0].name"
      via: "map.addTilesetImage('modern-exteriors', 'modern-exteriors')"
      pattern: "addTilesetImage"
    - from: "Collision layer tiles"
      to: "ge_collide:true in tileset"
      via: "Grid Engine reads custom property at runtime"
      pattern: "ge_collide"
---

<objective>
Full-stack smoke test: verify the complete Phase 2 system works end-to-end in a browser. The map (Plan 02-01) and scene code (Plan 02-02) are built independently — this plan confirms they integrate correctly and the world is playable.

Purpose: Human verification is the only way to confirm pixel art looks right, movement feels like Pokemon, collision is accurate across the map, and the world reads as Miami.
Output: Phase 2 declared complete. Human confirms playability.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-overworld-map/02-01-SUMMARY.md
@.planning/phases/02-overworld-map/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build</name>
  <files></files>
  <action>
Run the full automated validation suite before the human smoke test:

1. Full test suite: `npm test` — all vitest tests must pass (map structure, spawn point, collision properties, layer names, ocean tiles)
2. TypeScript check: `npx tsc --noEmit` — must exit 0
3. Production build: `npm run build` — must exit 0 with dist/ produced
4. Dev server start: `npm run dev` — must start without errors on localhost:5173

If any check fails, diagnose and fix before proceeding to the checkpoint. Common integration issues to check:
- `addTilesetImage` first arg does not match `tilesets[0].name` in overworld.json — silent blank map
- OverworldScene layer names ("Ground", "Above", "Collision") don't match overworld.json layer names — Phaser silently skips mismatched layers
- Grid Engine `create()` called before `createLayer()` — character cannot move
- GID=0 voids in Ground layer walkable areas — Grid Engine treats empty as blocked
- PIPOYA sprite sheet row order mismatch — animation plays in wrong direction
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npm test 2>&1 | tail -20 && npx tsc --noEmit 2>&1 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All vitest tests pass, TypeScript exits 0, production build exits 0. Dev server starts successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — full playable world verification</name>
  <files></files>
  <action>
Human opens http://localhost:5173 and works through the full Phase 2 checklist to confirm the world is playable and reads as Miami.
  </action>
  <verify>
    <automated>MISSING — this is a human-only verification step; Task 1 covers all automated checks</automated>
  </verify>
  <done>Human approves all checklist items: spawn at south dock, 4-directional movement (arrows + WASD), walking animation + idle state, camera follow + clamp, collision on buildings/water/trees, all 6 zones reachable, Miami visual read with palm trees and ocean, scaffolding on Chalk Lab and VC Office.</done>
  <what-built>
Complete Phase 2 overworld: a 50x40 Miami-themed tilemap with player character, 4-directional grid movement (Arrow keys + WASD), camera tracking, and collision — all rendered at 2x zoom with pixel-perfect rendering.
  </what-built>
  <how-to-verify>
Open http://localhost:5173 in browser (run `npm run dev` if not already running).

Work through each check:

**1. Spawn**
- [ ] Character appears at south dock area (bottom center of map) facing north
- [ ] Character is visible and not a broken image or white square

**2. Movement — all 4 directions**
- [ ] Arrow keys move character one tile at a time (not free-roam sliding)
- [ ] WASD keys also move character
- [ ] Walking animation plays during movement (3-frame cycle visible)
- [ ] Releasing keys: character stops and shows idle standing frame (not frozen mid-stride)

**3. Camera**
- [ ] Camera follows character as you walk north
- [ ] Camera stops at map edges (does not scroll past the world boundary)
- [ ] Pixel art is crisp (no blurring/anti-aliasing artifacts)

**4. Collision — test each type**
- [ ] Walking into a building shell stops movement
- [ ] Walking into ocean tiles (east edge) stops movement — you cannot walk into the water
- [ ] Walking into a palm tree stops movement
- [ ] Path and plaza tiles are walkable

**5. Map geography — walk the world**
- [ ] Starting at dock, walking north reaches main street (paved path, building shells on either side)
- [ ] Continuing north reaches central plaza area
- [ ] Walking east from plaza approaches beach strip / ocean boundary
- [ ] Heights zone (north top of map) is reachable
- [ ] West side (Andres House, Thoven HQ area) is reachable

**6. Miami visual read**
- [ ] Palm trees visible along main street and west side (not generic oaks)
- [ ] Ocean/water tiles cover east edge
- [ ] Dock area looks like a dock entry point from south
- [ ] Chalk Lab and VC Office have scaffolding visual overlay distinguishing them from finished buildings

Report any issues found (broken collision zones, missing zones, wrong spawn position, animation glitches, camera problems).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any specific issues found (e.g., "ocean tiles not blocking", "camera overshoots north edge", "walking animation not playing")</resume-signal>
</task>

</tasks>

<verification>
All automated tests pass + human verifies the full checklist above.
</verification>

<success_criteria>
- All 11 requirement IDs complete: WORLD-01 through WORLD-08, CHAR-01 through CHAR-03
- Human approves: spawn, movement (arrows + WASD), walking animation, idle state, camera follow + clamp, collision on buildings/water/trees, all 6 zones reachable, Miami visual read, scaffolding on under-construction buildings
- `npm test` green, `npm run build` green, `npx tsc --noEmit` green
</success_criteria>

<output>
After completion, create `.planning/phases/02-overworld-map/02-03-SUMMARY.md` following the summary template.
</output>
