---
phase: 02-overworld-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/maps/overworld.json
  - tests/overworld-map.test.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements: [WORLD-01, WORLD-05, WORLD-06, WORLD-07, WORLD-08]

must_haves:
  truths:
    - "overworld.json parses as valid JSON and Phaser can load it as a Tiled tilemap"
    - "All 6 zones are spatially represented: dock (south), main street spine, central plaza, west side, east beach strip, heights (north)"
    - "Building shells for every building are placed and marked as blocking tiles"
    - "Ocean tiles cover the east edge (x=42-49) and are blocked (ge_collide:true)"
    - "Palm trees appear in zone-appropriate positions (not generic oaks)"
    - "Under-construction buildings (Chalk Lab, VC Office) have scaffolding tile overlay"
  artifacts:
    - path: "public/assets/maps/overworld.json"
      provides: "50x40 Tiled JSON map with Ground, Above, Collision layers"
      contains: "layers, tilesets, ge_collide properties"
    - path: "tests/overworld-map.test.ts"
      provides: "Automated structural checks for map JSON"
      exports: ["vitest tests"]
    - path: "vitest.config.ts"
      provides: "Vitest test runner config"
  key_links:
    - from: "overworld.json tilesets[0].tiles"
      to: "ge_collide blocking tiles"
      via: "Grid Engine reads ge_collide property at runtime"
      pattern: "ge_collide.*true"
    - from: "overworld.json layers[2].name"
      to: "Collision"
      via: "OverworldScene.createLayer('Collision')"
      pattern: "\"name\": \"Collision\""
---

<objective>
Author the 50×40 Miami-themed Tiled JSON overworld map programmatically and set up the Vitest test framework to verify map structure.

Purpose: The map is the world. Every subsequent phase adds content on top of it. Getting the geography, collision, and zone placement right now prevents expensive rework later.
Output: `public/assets/maps/overworld.json` (complete map data) + `tests/overworld-map.test.ts` (automated structural checks) + `vitest.config.ts` + vitest dev dependency.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-overworld-map/02-CONTEXT.md
@.planning/phases/02-overworld-map/02-RESEARCH.md

<interfaces>
<!-- Established asset path conventions from Phase 1 -->
<!-- All game assets go in public/assets/ — never src/assets/ -->
<!-- Vite hashes src/ files, breaking Phaser's string-based loader -->

Tileset asset key used in BootScene (Plan 02-02):
  phaserImageKey: "modern-exteriors"
  loadPath:       "assets/tilesets/modern-exteriors-32.png"

Map asset key used in BootScene (Plan 02-02):
  phaserMapKey: "overworld"
  loadPath:     "assets/maps/overworld.json"

Tiled JSON tileset name (must match addTilesetImage first arg exactly):
  tilesets[0].name: "modern-exteriors"

Map dimensions (locked):
  width:      50   (x: 0 west → 49 east)
  height:     40   (y: 0 north → 39 south)
  tilewidth:  32
  tileheight: 32

Zone coordinate ranges (from RESEARCH.md):
  Dock:          x=20-30, y=36-39  (south spawn)
  Ocean strip:   x=42-49, all y    (east boundary)
  Heights:       y=0-8             (north top)
  Main street:   x=22-28, full N-S spine
  Central plaza: x=20-35, y=16-24
  West side:     x=0-19
  Beach strip:   x=38-49, y=10-39

Layer names (must match createLayer calls in OverworldScene):
  Layer 1: "Ground"     (base terrain — grass, paths, water, sand)
  Layer 2: "Above"      (building facades, trees, fences — rendered above ground)
  Layer 3: "Collision"  (invisible; tiles with ge_collide:true block movement)

GID rules:
  GID 0 = empty (no tile; Grid Engine treats as BLOCKED by default)
  GID 1 = firstgid + localId 0  (first tile in tileset)
  Fill every walkable Ground tile with a real GID (never leave 0 in walkable areas)

Grid Engine collision:
  Blocking tiles are marked in tilesets[0].tiles array:
  { "id": <localTileId>, "properties": [{ "name": "ge_collide", "type": "bool", "value": true }] }
  Grid Engine reads this at runtime — NO Phaser arcade physics needed.
</interfaces>

<!-- LimeZu Modern Exteriors 32x32 tile layout reference -->
<!-- The tileset is a PNG grid; columns = imageWidth / 32 -->
<!-- Agents must download the PNG, count columns, and catalog which localTileIds -->
<!-- correspond to: ground/path, building walls, water, palm trees, scaffolding -->
<!-- This catalog drives all GID assignments in the map JSON -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest and create map structure tests</name>
  <files>vitest.config.ts, tests/overworld-map.test.ts, package.json</files>
  <action>
Install vitest as a dev dependency: `npm install -D vitest`

Create `vitest.config.ts` at project root:
```typescript
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: {
    environment: "node",
    globals: true,
    include: ["tests/**/*.test.ts"],
  },
});
```

Add test script to package.json scripts: `"test": "vitest run"` and `"test:watch": "vitest"`

Create `tests/overworld-map.test.ts` with the following test cases. Import the map JSON directly:
```typescript
import { describe, it, expect } from "vitest";
import mapData from "../public/assets/maps/overworld.json";
```

Test cases to implement (RED state — map JSON does not exist yet, tests will fail):

1. **Map dimensions** — `mapData.width === 50`, `mapData.height === 40`, `mapData.tilewidth === 32`, `mapData.tileheight === 32`

2. **Required layers exist** — map has exactly 3 layers named "Ground", "Above", "Collision" (in that order)

3. **Data array length** — each layer's `data.length === 50 * 40` (2000)

4. **No empty Ground tiles in walkable areas** — Ground layer data array has no GID=0 in the main path corridor (x=22-28 on the main street spine, y=5-35). Test: for each (x,y) in that range, `groundData[x + y*50] !== 0`

5. **Ocean tiles on east edge** — for x in 42..49, all y: the Collision layer marks those tiles as blocked. Since we cannot call `ge_collide` at test time, verify the Above layer GIDs in x=42-49 are all within the water tile GID range (to be determined when tileset is cataloged — use a placeholder range check and update GID constants after Task 2)

6. **Blocking tiles defined in tileset** — `mapData.tilesets[0].tiles.length > 0` (some tiles have ge_collide property)

7. **Dock spawn zone not blocked** — Collision layer at dock area (x=24-26, y=37-39) contains GID=0 (empty = walkable)

8. **Tileset name matches** — `mapData.tilesets[0].name === "modern-exteriors"` (must match addTilesetImage first arg exactly)

Note: Tests will be RED (fail) until Task 2 creates the map JSON. That is expected TDD behavior for this wave.
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npx vitest run tests/overworld-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>vitest config exists, test script in package.json, tests/overworld-map.test.ts exists with all 8 test cases. Tests will FAIL (RED) — this is correct, the map JSON doesn't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author overworld.json — complete 50x40 Miami map</name>
  <files>public/assets/maps/overworld.json, public/assets/tilesets/modern-exteriors-32.png</files>
  <behavior>
    - overworld.json parses as valid Tiled JSON (width=50, height=40, tilewidth=32)
    - Three layers present: Ground, Above, Collision — in that order
    - Ground layer data array length === 2000 (50*40), no GID=0 in walkable path zones
    - Tileset name field === "modern-exteriors"
    - Dock zone (x=24-26, y=37-39) is walkable (Collision layer = 0 there)
    - Ocean strip (x=42-49, all y) has Above-layer water tiles and Collision-layer blocking tiles
    - Palm tree tiles appear in west side, plaza, and along main street (not the east ocean strip)
    - Blocking tile local IDs are listed in tilesets[0].tiles with ge_collide:true
    - Under-construction buildings (Chalk Lab, VC Office) have a scaffolding tile layer in Above
    - All zone areas are filled — no large GID=0 voids in the Ground layer
  </behavior>
  <action>
STEP 1 — Download and catalog the tileset.

Download LimeZu Modern Exteriors 32x32 from itch.io (free tier) or use the pack if already available. Place the primary exterior spritesheet at `public/assets/tilesets/modern-exteriors-32.png`.

Inspect the PNG:
- Count columns: `imageWidth / 32`
- Count rows: `imageHeight / 32`
- Catalog which local tile IDs (0-based) correspond to each terrain category:
  - Path/ground tiles (walkable) — the base paving/grass/dirt tiles
  - Building wall tiles (blocking) — facade tiles, wall corners, door tiles
  - Water tiles (blocking) — ocean/sea tiles
  - Palm tree tiles (blocking) — trunk + canopy tiles
  - Scaffolding tiles — construction overlay tiles
  - Sand/beach tiles (walkable) — sandy ground near water
  - Dock tiles — pier/boardwalk tiles

Create a brief comment block at the top of the JSON (in a separate notes file `public/assets/maps/tileset-catalog.txt`) mapping category → local tile ID ranges. This is the reference for all GID assignments.

STEP 2 — Author overworld.json programmatically.

Write a Node.js script (`scripts/generate-map.ts` or directly author the JSON) to produce `public/assets/maps/overworld.json`. The JSON must follow this structure exactly (see RESEARCH.md Pattern 3 for full schema):

```
{
  "version": "1.6",
  "tiledversion": "1.11.0",
  "type": "map",
  "orientation": "orthogonal",
  "renderorder": "right-down",
  "width": 50, "height": 40,
  "tilewidth": 32, "tileheight": 32,
  "infinite": false,
  "nextlayerid": 4, "nextobjectid": 1,
  "tilesets": [{ "firstgid": 1, "name": "modern-exteriors", ... }],
  "layers": [Ground layer, Above layer, Collision layer]
}
```

STEP 3 — Place tiles per zone layout.

Use this zone geography (y=0 is NORTH, y=39 is SOUTH):

**Ground layer (fill entire map — no GID=0 anywhere):**
- Default everywhere: path/grass ground tile
- Main street spine (x=22-28, all y): paved path tile
- Dock area (x=18-32, y=34-39): dock/pier tile
- Beach strip (x=38-41, y=5-39): sandy ground tile
- Ocean (x=42-49, all y): water tile (blocking — also mark in tileset tiles)
- Plaza (x=20-35, y=16-22): plaza/paved tile

**Above layer (building shells and trees):**

Buildings — paint facades using building wall tiles, mark all building tile local IDs as ge_collide:true in tileset:
- Thoven HQ: x=10-17, y=14-22 (largest, west of main street)
- Starbucks Café: x=29-34, y=24-28 (east of main street, south)
- Chalk Lab: x=18-22, y=8-13 (west, under construction — add scaffolding overlay)
- Andres's House: x=6-12, y=16-22 (behind Thoven, northwest)
- Engineering Lab: x=38-44, y=2-8 (northeast, hidden)
- GitHub Library: x=38-44, y=12-18 (east side, beach-facing)
- Record Shop: x=29-33, y=10-14 (east of main street, north)
- Ventanita: x=23-27, y=26-29 (main street, south)
- VC Office: x=28-33, y=16-20 (central plaza east — under construction, add scaffolding)
- Music Room: x=3-8, y=10-14 (behind Andres house, basement entrance)
- Idea Graveyard: x=2-10, y=24-32 (southwest, overgrown path around it)
- Lookout Hill: x=20-30, y=0-6 (heights, top-center)
- Bulletin Board: x=23-25, y=30-31 (main street, near south)

Palm trees — place palm tree tiles along:
- Main street edges (x=21, x=29, y=5-35 sparse)
- Plaza border (x=20-35, y=15-16 sparse)
- West side scattered (x=5-18, y=5-30 sparse, avoid building footprints)
- Beach strip border (x=37-38, y=5-32 sparse)

Ocean east edge (x=42-49): water tiles in Above layer for visual fill.

Scaffolding on Chalk Lab (x=18-22, y=8-13) and VC Office (x=28-33, y=16-20): add a scaffolding tile overlay on top of building base tiles.

**Collision layer (invisible at runtime — marks blocked tiles):**
- Place a blocking tile GID (any tile marked ge_collide:true) at every (x,y) that is blocked
- Blocked: all building footprints, all ocean tiles (x=42-49), all palm tree positions, all sign/fence positions
- Walkable: main street spine, dock area, plaza center, beach sand strip (x=38-41)
- Dock spawn (x=24-26, y=37-39): explicitly walkable (Collision GID=0)

**Tileset ge_collide properties:**
In `tilesets[0].tiles`, list every local tile ID that is blocking with `ge_collide:true`. Minimum set:
- All building wall tile local IDs
- All water tile local IDs
- All palm tree tile local IDs
- All scaffolding tile local IDs

STEP 4 — Validate.

Run `npx vitest run tests/overworld-map.test.ts` — all 8 tests must pass (GREEN). Fix any structural issues (wrong layer names, data length mismatch, GID=0 in walkable path).
  </action>
  <verify>
    <automated>cd /Users/andresmartinez/andres-world && npx vitest run tests/overworld-map.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All 8 map structure tests pass GREEN. overworld.json is in public/assets/maps/ with the correct 50x40 Tiled JSON format, three named layers, ge_collide properties on blocking tiles, all zones placed per the design doc geography, and tileset-catalog.txt documents the GID mapping.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/overworld-map.test.ts` — all 8 tests pass
- `node -e "JSON.parse(require('fs').readFileSync('public/assets/maps/overworld.json','utf8')); console.log('valid JSON')"` — no parse error
- Map JSON has `width:50, height:40, tilewidth:32` in top-level fields
- `tilesets[0].name === "modern-exteriors"` — checked in test
- Three layers named exactly: Ground, Above, Collision
- `tilesets[0].tiles.length > 0` — at least some ge_collide properties defined
</verification>

<success_criteria>
- Vitest installed and `npm test` runs the test suite
- `tests/overworld-map.test.ts` has 8 passing tests
- `public/assets/maps/overworld.json` exists with valid Tiled JSON structure
- `public/assets/tilesets/modern-exteriors-32.png` exists
- `public/assets/maps/tileset-catalog.txt` documents localTileId → terrain category mapping
- All zone areas populated per design doc ASCII layout
- Ocean east edge (x=42-49) is blocked
- Dock spawn zone (x=24-26, y=37-39) is walkable
</success_criteria>

<output>
After completion, create `.planning/phases/02-overworld-map/02-01-SUMMARY.md` following the summary template.
</output>
