---
phase: 03.3-map-layout-design
plan: 02
type: execute
wave: 2
depends_on:
  - "03.3-01"
files_modified:
  - scripts/generate-map.ts
  - src/game/config/npcs.ts
  - public/assets/maps/overworld.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Andres's House footprint is x=6-9 (not x=6-12) — the overlap with Thoven HQ is gone"
    - "Chalk Lab footprint is x=18-21 — the east edge no longer touches main street at x=22"
    - "Thoven HQ is the largest building at 10x9 tiles (x=10-19, y=14-22)"
    - "Ventanita, Record Shop, and Bulletin Board are proportionally shrunk"
    - "No NPC startPosition sits inside a modified building footprint"
    - "All Plan 01 failing assertions now pass"
  artifacts:
    - path: "scripts/generate-map.ts"
      provides: "Updated fillRect calls for 6 building footprints in Above + Collision layers"
      contains: "fillRect(data, 6, 16, 9, 22"
    - path: "src/game/config/npcs.ts"
      provides: "Updated startPosition for Dad (x=9→x=10) and any other displaced NPCs"
      contains: "startPosition: { x: 10, y: 18 }"
    - path: "public/assets/maps/overworld.json"
      provides: "Regenerated map with corrected building footprints"
  key_links:
    - from: "scripts/generate-map.ts"
      to: "public/assets/maps/overworld.json"
      via: "npx tsx scripts/generate-map.ts"
      pattern: "tsx scripts/generate-map.ts"
    - from: "src/game/config/npcs.ts"
      to: "scripts/generate-map.ts"
      via: "NPC startPositions must be outside building collision footprints"
      pattern: "startPosition"
---

<objective>
Fix all locked building footprint overlaps and proportion corrections in generate-map.ts, then update NPC coordinates in npcs.ts to match the new building boundaries.

Purpose: Lock in the permanent tile coordinates that Phase 4+ interior scenes will use for entrance/exit tile detection. Getting Andres's House at x=6-9 wrong now means auditing InteriorBaseScene transition logic later.

Output: generate-map.ts with corrected fillRect calls for 6 buildings, npcs.ts with verified NPC positions, regenerated overworld.json passing all Plan 01 assertions.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.3-map-layout-design/03.3-CONTEXT.md
@.planning/phases/03.3-map-layout-design/03.3-RESEARCH.md
@.planning/phases/03.3-map-layout-design/03.3-01-SUMMARY.md

<interfaces>
<!-- Key patterns extracted from codebase. Executor uses these directly. -->

From scripts/generate-map.ts — three-layer pattern (MUST update all three):
```typescript
// buildAboveLayer() — visual tile
fillRect(data, x1, y1, x2, y2, BUILDING_GID);

// buildCollisionLayer() — blocking mask (uses BLOCK = BUILDING_GID)
const BLOCK = BUILDING_GID;
fillRect(data, x1, y1, x2, y2, BLOCK);

// buildGroundLayer() — no change needed for building footprints
// (grass base stays under building — buildings sit on top in Above layer)
```

Current building footprints → Phase 3.3 target footprints:
| Building | Current (Above + Collision) | Target |
|----------|----------------------------|--------|
| Andres's House | fillRect(data, 6, 16, 12, 22, ...) | fillRect(data, 6, 16, 9, 22, ...) |
| Chalk Lab | fillRect(data, 18, 8, 22, 13, ...) + scaffold(18,8,22,9) | fillRect(data, 18, 8, 21, 13, ...) + scaffold(18,8,21,9) |
| Thoven HQ | fillRect(data, 10, 14, 17, 22, ...) | fillRect(data, 10, 14, 19, 22, ...) |
| Ventanita | fillRect(data, 23, 26, 27, 29, ...) | fillRect(data, 23, 26, 25, 29, ...) |
| Record Shop | fillRect(data, 29, 10, 33, 14, ...) | fillRect(data, 29, 10, 32, 14, ...) |
| Bulletin Board | fillRect(data, 23, 30, 25, 31, ...) | fillRect(data, 23, 30, 24, 31, ...) |

NPC positions to verify/update (from src/game/config/npcs.ts):
| NPC | Current startPosition | Issue | Action |
|-----|----------------------|-------|--------|
| dad | x=9, y=18 | x=9 is new east wall of Andres's House (x=6-9 includes x=9) | Move to x=10, y=18 |
| keri | x=12, y=24 | Was inside old house (x=6-12). New house ends at x=9. x=12 is now open space. | No change needed — already outside |
| brian-chesky | x=11, y=24 | x=11 is open space after house shrinks. | No change needed |
| michael-seibel | x=14, y=24 | Thoven expands to x=10-19. x=14 is now inside expanded Thoven HQ footprint (y=14-22). y=24 is south of Thoven (y≤22). | No change needed — y=24 is outside |
| dog-1 | x=7, y=20 | x=7 is inside new Andres's House (x=6-9, y=16-22). y=20 inside. | This is an interior NPC — acceptable on overworld for now |
| patrick-collison | x=42, y=5 | Inside Engineering Lab (x=38-44, y=2-8) — unchanged building | Already inside building — interior NPC, acceptable |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update 6 building footprints in generate-map.ts (Above + Collision layers)</name>
  <files>scripts/generate-map.ts</files>
  <behavior>
    After changes, the following must hold in the regenerated overworld.json:

    - Above layer at x=10, y=18 (formerly inside Andres's House x=6-12) must NOT be BUILDING_GID (7689)
    - Above layer at x=6, y=18 (still inside new Andres's House x=6-9) must be BUILDING_GID (7689)
    - Above layer at x=22, y=10 (formerly inside Chalk Lab x=18-22) must NOT be BUILDING_GID (7689)
    - Above layer at x=18, y=18 (inside new Thoven HQ x=10-19) must be BUILDING_GID (7689)
    - These are the Plan 01 assertions that were RED — they must now be GREEN
  </behavior>
  <action>
    Make exactly these changes in generate-map.ts. Do not touch any other fillRect calls.

    In buildAboveLayer():
    1. Andres's House: change `fillRect(data, 6, 16, 12, 22, BUILDING_GID)` → `fillRect(data, 6, 16, 9, 22, BUILDING_GID)`
    2. Chalk Lab base: change `fillRect(data, 18, 8, 22, 13, BUILDING_GID)` → `fillRect(data, 18, 8, 21, 13, BUILDING_GID)`
    3. Chalk Lab scaffold: change `fillRect(data, 18, 8, 22, 9, SCAFFOLD_GID)` → `fillRect(data, 18, 8, 21, 9, SCAFFOLD_GID)`
    4. Thoven HQ: change `fillRect(data, 10, 14, 17, 22, BUILDING_GID)` → `fillRect(data, 10, 14, 19, 22, BUILDING_GID)`
    5. Ventanita: change `fillRect(data, 23, 26, 27, 29, BUILDING_GID)` → `fillRect(data, 23, 26, 25, 29, BUILDING_GID)`
    6. Record Shop: change `fillRect(data, 29, 10, 33, 14, BUILDING_GID)` → `fillRect(data, 29, 10, 32, 14, BUILDING_GID)`
    7. Bulletin Board: change `fillRect(data, 23, 30, 25, 31, BUILDING_GID)` → `fillRect(data, 23, 30, 24, 31, BUILDING_GID)`

    In buildCollisionLayer() — same 6 footprints, same changes (BLOCK = BUILDING_GID):
    1. Andres's House: `fillRect(data, 6, 16, 12, 22, BLOCK)` → `fillRect(data, 6, 16, 9, 22, BLOCK)`
    2. Chalk Lab: `fillRect(data, 18, 8, 22, 13, BLOCK)` → `fillRect(data, 18, 8, 21, 13, BLOCK)`
    3. Thoven HQ: `fillRect(data, 10, 14, 17, 22, BLOCK)` → `fillRect(data, 10, 14, 19, 22, BLOCK)`
    4. Ventanita: `fillRect(data, 23, 26, 27, 29, BLOCK)` → `fillRect(data, 23, 26, 25, 29, BLOCK)`
    5. Record Shop: `fillRect(data, 29, 10, 33, 14, BLOCK)` → `fillRect(data, 29, 10, 32, 14, BLOCK)`
    6. Bulletin Board: `fillRect(data, 23, 30, 25, 31, BLOCK)` → `fillRect(data, 23, 30, 24, 31, BLOCK)`

    After edits, regenerate: `npx tsx scripts/generate-map.ts`

    Then run tests: `npx vitest run tests/overworld-map.test.ts`

    The 4 building-footprint assertions from Plan 01 must now PASS. The tall grass, cross-street, and boardwalk assertions remain RED (those are Plan 03 work).
  </action>
  <verify>
    <automated>npx tsx scripts/generate-map.ts && npx vitest run tests/overworld-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    generate-map.ts has 13 updated fillRect calls (7 in Above, 6 in Collision). overworld.json regenerated. Vitest shows 4 more passing assertions than before Task 1 (the building footprint assertions are GREEN). Tall grass / cross-street / boardwalk assertions still fail (expected — Plan 03 work).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update NPC coordinates in npcs.ts for modified building boundaries</name>
  <files>src/game/config/npcs.ts, public/assets/maps/overworld.json</files>
  <action>
    Apply NPC coordinate fixes to src/game/config/npcs.ts:

    1. Dad (id: "dad"): Change `startPosition: { x: 9, y: 18 }` → `startPosition: { x: 10, y: 18 }`
       Reason: x=9 is now the east wall of Andres's House (x=6-9). Dad is an interior NPC (Phase 4) but appears on the overworld currently at the wall edge. Moving to x=10 places him just outside the east wall — accessible by the player.

    2. Cross-check all other NPC startPositions against the updated building footprints listed in the interfaces block above. If any NPC is found inside a collision-blocked zone (other than the two already identified as intentional interior NPCs — dog-1 at x=7,y=20 and patrick-collison at x=42,y=5), move them to the nearest walkable adjacent tile.

    The west palm collision loop blocks certain positions (x=21, y=5/9/13/17/etc.). Confirm no NPC is on a palm collision tile.

    After updating npcs.ts, run the full test suite to confirm no regressions:
    `npx vitest run`
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>
    Dad's startPosition is x=10, y=18. All other NPC positions verified against new building boundaries. Full vitest suite passes (all previously-passing tests still pass). npcs.ts committed.
  </done>
</task>

</tasks>

<verification>
- `npx tsx scripts/generate-map.ts` runs without errors
- `npx vitest run tests/overworld-map.test.ts` shows building footprint assertions passing, tall grass / cross-street assertions still failing
- `npx vitest run` full suite passes (no regressions from NPC config change)
- generate-map.ts has updated fillRect calls for all 6 buildings in both Above and Collision layers
- npcs.ts has Dad at startPosition x=10, y=18
</verification>

<success_criteria>
Building footprints match the locked coordinates from CONTEXT.md. NPC positions are outside collision zones. The permanent tile coordinates for Phase 4+ interior entrance detection are locked: Andres's House x=6-9, Thoven HQ x=10-19, Chalk Lab x=18-21.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-map-layout-design/03.3-02-SUMMARY.md`
</output>
