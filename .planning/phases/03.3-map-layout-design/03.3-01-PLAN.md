---
phase: 03.3-map-layout-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/overworld-map.test.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "TALL_GRASS_GID is visually confirmed — Andres has hovered tileset-preview.html and approved a non-transparent grass variant"
    - "All new geometry assertions are in overworld-map.test.ts and fail (RED) against current overworld.json"
  artifacts:
    - path: "tests/overworld-map.test.ts"
      provides: "RED assertions for building footprint changes, tall grass presence, tall grass gap walkability, cross-streets, boardwalk, dock welcome zone"
      contains: "TALL_GRASS_GID"
  key_links:
    - from: "tests/overworld-map.test.ts"
      to: "public/assets/maps/overworld.json"
      via: "import mapData from overworld.json"
      pattern: "import mapData from"
---

<objective>
Establish the TDD RED gate for all Phase 3.3 geometry changes.

Purpose: Force every geometry change in Plans 02 and 03 to be test-driven. No building footprint or zone change touches generate-map.ts until there is a failing assertion for it. TALL_GRASS_GID is human-confirmed before the assertion is written — preserving the mandatory ✅ visual confirm workflow that caught the Phase 3.1 transparent-tile bug.

Output: overworld-map.test.ts with 8 new failing assertions + confirmed TALL_GRASS_GID candidate committed to TILE-CATALOG.md.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-map-layout-design/03.3-CONTEXT.md
@.planning/phases/03.3-map-layout-design/03.3-RESEARCH.md
@.planning/TILE-CATALOG.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from existing codebase. -->

From tests/overworld-map.test.ts (existing pattern to follow):
```typescript
// Tile lookup pattern
const aboveData: number[] = mapData.layers[1].data as number[];
const idx = x + y * WIDTH;
expect(aboveData[idx]).toBe(GID_VALUE);

// Collision walkable check
const collisionData: number[] = mapData.layers[2].data as number[];
expect(collisionData[idx]).toBe(0); // 0 = walkable
```

From scripts/generate-map.ts (GID derivation):
```typescript
const TERRAIN_FIRSTGID = 1;
const TERRAIN_COLS = 32;
function tileGid(firstgid: number, cols: number, row: number, col: number): number {
  return firstgid + (row * cols + col);
}
// TALL_GRASS candidate: Terrains row=7, col=0 → GID 225
// tileGid(1, 32, 7, 0) = 1 + (7*32+0) = 1 + 224 = 225
```

Phase 3.3 locked building coordinates (from CONTEXT.md):
- Andres's House new footprint: x=6-9, y=16-22 (shrunk from x=6-12)
- Chalk Lab new footprint: x=18-21, y=8-13 (shrunk from x=18-22)
- Thoven HQ expanded footprint: x=10-19, y=14-22 (expanded from x=10-17)
- Ventanita new footprint: x=23-25, y=26-29 (shrunk from x=23-27)
- Record Shop new footprint: x=29-32, y=10-14 (shrunk from x=29-33)
- Bulletin Board new footprint: x=23-24, y=30-31 (shrunk from x=23-25)
- West cross-street: PATH_GID at x=9-21, y=23 (south of all NW buildings)
- East boardwalk: PATH_GID at x=37, y=0-39 (vertical spine)
- Dock welcome zone: walkable at x=24-26, y=36
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm TALL_GRASS_GID via tileset-preview.html</name>
  <what-built>
    Nothing built yet. This checkpoint gates the test-writing step.

    Candidate: Terrains tileset row=7, col=0 → GID 225 (a green grass variant confirmed non-transparent via TILE-CATALOG.md research).

    If GID 225 looks identical to regular GRASS_GID (row=6, col=0 → GID 193), the next candidate is Terrains row=8, col=0 → GID 257. A third option if neither reads as distinct: Garden tileset row=1, col=0 → GID 12801 (lime green foliage, more visually distinct but different aesthetic).
  </what-built>
  <how-to-verify>
    1. Open public/tileset-preview.html in your browser (or run `npx vite` and visit /tileset-preview.html)
    2. Load the Terrains tileset (1_Terrains_and_Fences_16x16.png)
    3. Hover over row=7, col=0 — confirm the GID displayed is 225 and the tile is a visually distinct darker/denser grass (not transparent, not identical to row=6)
    4. If row=7 col=0 is not distinct enough, try row=8, col=0 (GID 257)
    5. Pick the best candidate and note its GID
    6. Add a ✅ entry to .planning/TILE-CATALOG.md:
       `| TALL_GRASS_GID | {tileset} | row={r} col={c} | GID {gid} | ✅ | dark/dense grass variant |`
  </how-to-verify>
  <resume-signal>
    Type the confirmed TALL_GRASS_GID number (e.g., "225" or "257") so the executor can use it in Task 2.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test assertions for all Phase 3.3 geometry</name>
  <files>tests/overworld-map.test.ts</files>
  <behavior>
    Use the TALL_GRASS_GID confirmed in Task 1 as the expected value in the tall grass assertions.

    New assertions to add (all must FAIL against current overworld.json):

    1. Andres's House east boundary shrunk — tile at x=10, y=18 (formerly inside house) must NOT be BUILDING_GID in Above layer
    2. Andres's House west portion still present — tile at x=6, y=18 (new footprint) must be BUILDING_GID in Above layer
    3. Chalk Lab east boundary shrunk — tile at x=22, y=10 (formerly inside Chalk Lab) must NOT be BUILDING_GID in Above layer
    4. Thoven HQ expanded — tile at x=18, y=18 (new footprint, currently outside old x=10-17) must be BUILDING_GID in Above layer
    5. Tall grass present — Above layer at x=10, y=27 (Idea Graveyard east boundary) must be TALL_GRASS_GID
    6. Tall grass gap is walkable — Collision layer at x=10, y=27 must be 0 (the gap tile in the east wall of Idea Graveyard is carved out)
    7. West cross-street — Ground layer at x=15, y=23 must be PATH_GID (294)
    8. East boardwalk — Ground layer at x=37, y=10 must be PATH_GID (294) (confirming vertical boardwalk spine at y=10, which is currently blocked by palm loop)

    Important: x=10, y=27 is the gap tile position in the east wall of the Idea Graveyard tall grass field. The field covers x=2-10, y=24-32. The gap in the east boundary is at x=10, y=27-28. One assertion checks the gap tile (0 in collision), and another checks the surrounding tall grass tile (TALL_GRASS_GID in above layer at a non-gap position, e.g. x=3, y=27).

    Revise assertion 5 to: Above layer at x=3, y=27 (inside Idea Graveyard field, not the gap) must be TALL_GRASS_GID.
    Assertion 6 remains: Collision layer at x=10, y=27 (the gap tile) must be 0.
  </behavior>
  <action>
    Add a new describe block "Phase 3.3 — geometry changes" to tests/overworld-map.test.ts. Do NOT modify existing assertions. Add 8 new it() blocks using the TALL_GRASS_GID value confirmed in Task 1.

    The TALL_GRASS_GID value is provided by the user's resume signal from Task 1. Use that exact value in the assertion.

    After adding assertions, run: `npx vitest run tests/overworld-map.test.ts`

    All 8 new assertions MUST fail (RED). If any pass against current overworld.json, the assertion is testing the wrong coordinate or GID — fix the assertion until it fails correctly.

    Existing assertions must still pass.
  </action>
  <verify>
    <automated>npx vitest run tests/overworld-map.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    8 new assertions in tests/overworld-map.test.ts, all failing. Existing 9 assertions still passing. Output shows "9 passed, 8 failed" (or similar count).
  </done>
</task>

</tasks>

<verification>
- TILE-CATALOG.md has a new ✅ entry for TALL_GRASS_GID
- tests/overworld-map.test.ts has 8 new failing assertions in a "Phase 3.3 — geometry changes" describe block
- `npx vitest run tests/overworld-map.test.ts` shows all original tests passing, all new tests failing
</verification>

<success_criteria>
TALL_GRASS_GID is human-confirmed and in TILE-CATALOG.md. The test file is the contract for Plans 02 and 03 — no geometry change is valid unless it makes a failing assertion pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-map-layout-design/03.3-01-SUMMARY.md`
</output>
