---
phase: 03.2-map-visual-design
plan: "03"
type: execute
wave: 3
depends_on:
  - "03.2-02"
files_modified: []
autonomous: false
requirements:
  - ART-06
  - ART-07
  - ART-08

must_haves:
  truths:
    - "Building shells are visually distinct colored tiles (not transparent) on the overworld"
    - "Palm trees render as visible pixel-art palms with frond tile above trunk tile"
    - "Chalk Lab and VC Office show visible scaffolding overlay tiles"
    - "Player cannot walk through palm trees (collision blocks correctly)"
  artifacts:
    - path: "public/assets/maps/overworld.json"
      provides: "Confirmed correct overworld map with visible art tiles"
      contains: "7689"
  key_links:
    - from: "src/game/scenes/Boot.ts"
      to: "public/assets/maps/overworld.json"
      via: "Phaser tilemap loader"
      pattern: "this.load.tilemapTiledJSON"
---

<objective>
Human visual verification that the map renders correctly in the browser. Andres walks to key locations to confirm buildings, palm trees, and scaffolding are visible pixel art (not transparent). Also verifies palm collision blocks movement.

Purpose: Automated tests confirm the JSON data is correct. This checkpoint confirms Phaser actually renders the tiles as visible art in the browser.

Output: Human approval ("approved") or bug report describing what is still transparent or incorrect.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for visual verification</name>
  <files></files>
  <action>
Run the dev server so Andres can do the visual check:
```bash
npm run dev
```
The server starts at http://localhost:5173. Confirm it starts without errors. Output the URL.
  </action>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -20</automated>
  </verify>
  <done>Dev server running at http://localhost:5173, no build errors in console output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Fixed 3 transparent-tile bugs in generate-map.ts. The overworld map now uses:
- BUILDING_GID = 7689 (solid wall tile, buildings row=41 col=8)
- PALM_GID = 2770 (palm fronds, beach row=12 col=17)
- PALM_TRUNK_GID = 2834 (palm trunk, beach row=14 col=17)
- SCAFFOLD_GID = 3598 (X-brace scaffold, beach row=38 col=13)

Palm trees now render as 2-tile sprites (frond tile + trunk tile one row below).
Scaffolding overlays Chalk Lab (x=18-22, y=8-9) and VC Office (x=28-33, y=16-17).
  </what-built>
  <how-to-verify>
Open http://localhost:5173 in your browser. Walk the character to verify:

1. **Buildings visible (ART-06)**
   - Walk toward Thoven HQ (large building, west of main path, around x=10-17, y=14-22)
   - You should see a solid colored tile (not transparent/invisible) filling the building footprint
   - Expected: brownish or grey wall tiles from the LimeZu buildings tileset

2. **Palm trees visible (ART-07)**
   - Walk north along main street — palms should be visible on both sides of the path
   - Each palm should show a frond tile (top) with a trunk tile directly below it
   - Expected: green tropical palm sprite, 2 tiles tall

3. **Palm collision works (ART-07)**
   - Try to walk through a palm tree — character should be blocked
   - Expected: movement stops at the palm tile, cannot pass through

4. **Scaffolding visible (ART-08)**
   - Walk to Chalk Lab area (upper section, northwest of main street, around x=18-22, y=8-13)
   - The top 2 rows of the building should show a visible X-brace scaffold overlay
   - Expected: steel X-frame construction pattern visible over the building shell

If something looks wrong, describe: which tile type is still transparent, and what you see instead.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 visual checks pass. Or describe what's still broken (e.g., "palms still transparent", "scaffold not visible at Chalk Lab").</resume-signal>
</task>

</tasks>

<verification>
Human visual confirmation that buildings, palms (2-tile), palm collision, and scaffolding all render correctly in the browser at http://localhost:5173.
</verification>

<success_criteria>
- Buildings at Thoven HQ and other locations render as visible solid wall tiles (not transparent)
- Palm trees are visible as 2-tile pixel-art palms (frond above, trunk below) along main street and beach strip
- Walking into a palm tree blocks the player (collision works)
- Chalk Lab top rows show visible X-brace scaffolding overlay
- Human types "approved" to confirm all four checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-map-visual-design/03.2-03-SUMMARY.md`
</output>
