# Phase 3.2: Map Visual Design - Context

**Gathered:** 2026-03-09
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix the three critical transparent-tile bugs in `generate-map.ts` (BUILDING_GID, PALM_GID, SCAFFOLD_GID all point at row=0 transparent tiles) and make the overworld visually correct: real grass, earthy paths, teal ocean, sandy beach, dock, building shells, palm trees, scaffolding overlays. Every GID update must reference a ✅ entry in TILE-CATALOG.md. No new features — this is a bug-fix + visual-correctness pass on the existing map.

</domain>

<decisions>
## Implementation Decisions

### Building tile composition
- **Solid fill only — single wall GID (7689) for all buildings** — confirmed ✅ in TILE-CATALOG.md
- Same tile for all building types; visual differentiation deferred to Phase 9 (Miami Art Deco assets)
- Multi-tile building facades (roof edge / wall face / base rows) deferred to a later phase — not Phase 3.2
- Scaffold over building shell — under-construction buildings (Chalk Lab, VC Office) get SCAFFOLD_GID (3598) overlaid on top of BUILDING_GID rows (existing approach confirmed)
- Building footprints and positions are CORRECT from Phase 2 — only GIDs change, no geometry changes

### Palm trees
- **2-tile palm**: fronds at GID 2770 (beach row=12, col=17, ✅) placed at the current tile position; trunk at GID 2834 (beach row=14, col=17, ✅) placed one row below the fronds tile
- Keep existing palm placement positions from Phase 2 — only GIDs change
- Palms have collision (ge_collide:true) — player cannot walk through them

### Ground tiles
- **Single GRASS_GID (193)** fills the ground — no variety or random scattering for Phase 3.2
- Ground polish (transition tiles, texture variety) deferred to Phase 9

### Water and beach
- **Solid WATER_GID (186)** fills ocean zone — no shore/transition tiles for Phase 3.2
- **Solid SAND_GID (2433)** fills beach strip — no sand-to-water transition
- Water is impassable — ge_collide:true on water tiles (confirm existing Phase 2 collision config)
- Animated water deferred to Phase 9

### Claude's Discretion
- How to handle the palm trunk row placement in `generate-map.ts` — whether to create a second pass over palm positions or embed trunk placement inline with frond placement
- Exact test assertions to update in generate-map tests (GID value checks)
- Whether to add a `PALM_TRUNK_GID` constant or derive it inline

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `scripts/generate-map.ts`: All tile GID constants defined at top with tileGid() helper. Fix is surgical: update the row/col params for BUILDING_GID, PALM_GID, SCAFFOLD_GID. No architecture change needed.
- `tileGid(firstgid, cols, row, col)` helper already exists — use it for all new GID derivations
- GRASS_GID, PATH_GID, WATER_GID, SAND_GID, DOCK_GID are already correct from Phase 3.1

### Established Patterns
- **TILE-CATALOG.md is the source of truth** — every GID used in generate-map.ts must have a matching ✅ entry
- Tileset firstgid chain (TERRAIN=1, BEACH=2369, BUILDINGS=6369, GARDEN=12769, WORKSITE=19041) is locked — do not change
- TDD pattern from Phase 3.1: update test assertions to new expected GID values before changing generate-map.ts

### Integration Points
- `generate-map.ts` → `public/assets/maps/overworld.json` (output file)
- Phaser loads overworld.json in Boot.ts — no Boot.ts changes needed for GID updates
- Grid Engine tile collision is set via tileset tile properties in generate-map.ts's `buildXxxTileProperties()` functions — PALM_GID collision needs to be added here

</code_context>

<specifics>
## Specific Ideas

- "Keep it as simple as possible" — Andres explicitly flagged this as the guiding principle for 3.2
- Phase 3.2 is a targeted surgical fix, not a redesign. The work is: (1) update 3 GID constants, (2) add 2-tile palm logic, (3) confirm collision on water/palms
- Multi-tile building facades noted as a future need but explicitly deferred out of Phase 3.2 scope

</specifics>

<deferred>
## Deferred Ideas

- Multi-tile building facades (roof edge / wall face / base rows) — future phase before Phase 9
- 2-3 building tile variants for visual differentiation — Phase 9 with Miami Art Deco assets
- Ground tile variety (grass variants at rows 7/8, transition tiles) — Phase 9
- Shore/sand-to-water transition tiles — Phase 9
- Animated water tiles — Phase 9 (already in Phase 2 deferred list)

</deferred>

---

*Phase: 03.2-map-visual-design*
*Context gathered: 2026-03-09*
