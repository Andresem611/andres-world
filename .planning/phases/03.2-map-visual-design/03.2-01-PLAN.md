---
phase: 03.2-map-visual-design
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/overworld-map.test.ts
autonomous: true
requirements:
  - ART-06
  - ART-07
  - ART-08

must_haves:
  truths:
    - "npm test fails with 4 new failing assertions (RED gate confirmed)"
    - "Existing 7 test assertions continue to pass (no regression)"
    - "Each new assertion maps to a specific confirmed GID from TILE-CATALOG.md"
  artifacts:
    - path: "tests/overworld-map.test.ts"
      provides: "4 new it() blocks for building, palm frond, palm trunk, and scaffold GID assertions"
      contains: "expect(aboveData[idx]).toBe(7689)"
  key_links:
    - from: "tests/overworld-map.test.ts"
      to: "public/assets/maps/overworld.json"
      via: "import mapData from overworld.json"
      pattern: "mapData.layers\\[1\\].data"
---

<objective>
Add 4 failing test assertions to `tests/overworld-map.test.ts` that target the three critical transparent-tile bugs: BUILDING_GID (expects 7689), PALM_GID frond (expects 2770), PALM_TRUNK_GID (expects 2834), and SCAFFOLD_GID (expects 3598).

Purpose: Establish the RED gate before fixing generate-map.ts. Tests must fail now and pass after Plan 02 regenerates overworld.json with correct GIDs. This is the TDD contract established in Phase 3.1.

Output: 4 new `it()` blocks appended to the existing describe block in overworld-map.test.ts. No new files.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TILE-CATALOG.md

<interfaces>
<!-- Existing test file structure — append inside the describe block, after line 81 (closing brace of last it()) and before line 82 (closing brace of describe()). -->

From tests/overworld-map.test.ts (current state):
```typescript
import { describe, it, expect } from "vitest";
import mapData from "../public/assets/maps/overworld.json";

const WIDTH = 50;
// ... existing tests ...
describe("overworld.json — map structure", () => {
  // 7 existing it() blocks
  // ADD 4 NEW it() BLOCKS HERE (before closing brace of describe)
});
```

From .planning/TILE-CATALOG.md (confirmed GIDs):
- BUILDING_GID = 7689  (buildings row=41, col=8 — confirmed ✅ by Andres)
- PALM_GID = 2770      (beach row=12, col=17 — confirmed ✅ by Andres)
- PALM_TRUNK_GID = 2834 (beach row=14, col=17 — confirmed ✅ by Andres)
- SCAFFOLD_GID = 3598   (beach row=38, col=13 — confirmed ✅ by Andres)

From generate-map.ts (known placement positions):
- Thoven HQ Above layer: x=10-17, y=14-22 (filled with BUILDING_GID)
- Main street west palm frond: x=21, y=5
- Main street west palm trunk: x=21, y=6 (one row below frond)
- Chalk Lab scaffold overlay: x=18-22, y=8-9 (top rows of Chalk Lab Above layer)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4 failing GID assertions to overworld-map.test.ts</name>
  <files>tests/overworld-map.test.ts</files>
  <behavior>
    - Test 1 (ART-06): Above layer at Thoven HQ top-left (x=10, y=14) has GID=7689. Currently fails because overworld.json has BUILDING_GID=6369 (transparent row=0).
    - Test 2 (ART-07 frond): Above layer at x=21, y=5 has GID=2770. Currently fails because overworld.json has PALM_GID=12769 (garden transparent row=0).
    - Test 3 (ART-07 trunk): Above layer at x=21, y=6 has GID=2834. Currently fails because overworld.json has no palm trunk tile at all (trunk logic not yet implemented).
    - Test 4 (ART-08): Above layer at Chalk Lab scaffold position x=18, y=8 has GID=3598. Currently fails because overworld.json has SCAFFOLD_GID=19041 (worksite transparent row=0).
  </behavior>
  <action>
Append 4 new `it()` blocks inside the existing `describe("overworld.json — map structure", () => { ... })` block in `tests/overworld-map.test.ts`. Place them after the last existing test and before the closing `});` of the describe.

Use this exact code (from VALIDATION.md + RESEARCH.md confirmed assertions):

```typescript
  it("Above layer at Thoven HQ footprint has BUILDING_GID (7689)", () => {
    const aboveData: number[] = mapData.layers[1].data as number[];
    // Thoven HQ: x=10-17, y=14-22 — top-left corner tile
    const idx = 10 + 14 * WIDTH;
    expect(aboveData[idx]).toBe(7689);
  });

  it("Above layer has palm frond tile (GID 2770) at main street west palm position", () => {
    const aboveData: number[] = mapData.layers[1].data as number[];
    // Main street west palm frond at x=21, y=5
    const idx = 21 + 5 * WIDTH;
    expect(aboveData[idx]).toBe(2770);
  });

  it("Above layer has palm trunk (GID 2834) one row below the palm frond", () => {
    const aboveData: number[] = mapData.layers[1].data as number[];
    // Palm trunk at x=21, y=6 (one row below frond at y=5)
    const idx = 21 + 6 * WIDTH;
    expect(aboveData[idx]).toBe(2834);
  });

  it("Above layer at Chalk Lab top rows has SCAFFOLD_GID (3598)", () => {
    const aboveData: number[] = mapData.layers[1].data as number[];
    // Chalk Lab scaffold overlay: x=18-22, y=8-9
    const idx = 18 + 8 * WIDTH;
    expect(aboveData[idx]).toBe(3598);
  });
```

After adding the assertions, run `npm test`. The 4 new tests MUST FAIL. The existing 7 tests MUST PASS. If all 11 pass, the existing overworld.json already has the correct GIDs — stop and report this to the user before continuing.

Do NOT change generate-map.ts in this task. Do NOT regenerate overworld.json in this task. The RED state is the deliverable.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <done>4 new test assertions added to overworld-map.test.ts. Running npm test shows exactly 4 failing tests (the new assertions) and 7 passing tests (the existing ones). No changes to generate-map.ts or overworld.json.</done>
</task>

</tasks>

<verification>
npm test — must show 4 failing tests (new GID assertions) + 7 passing tests (existing assertions). Total: 11 tests, 4 fail, 7 pass.
</verification>

<success_criteria>
- overworld-map.test.ts has 11 total it() blocks (7 existing + 4 new)
- npm test output shows 4 failures: building GID, palm frond GID, palm trunk GID, scaffold GID
- Existing 7 tests all pass (no regression)
- No changes to scripts/generate-map.ts or public/assets/maps/overworld.json
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-map-visual-design/03.2-01-SUMMARY.md`
</output>
