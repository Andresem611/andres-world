---
phase: 03.3.1
slug: tiled-visual-map-design-pass
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 03.3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js one-liners + manual visual walk-through |
| **Config file** | none — validation scripts are inline commands |
| **Quick run command** | `node -e "const m=require('./public/assets/maps/overworld.json'); const above=m.layers.find(l=>l.name==='Above'); const bad=above.data.filter(x=>x===7689); console.log(bad.length===0?'✓ No flat BUILDING_GID':'✗ '+bad.length+' flat tiles remain')"` |
| **Full suite command** | `npm run dev` + manual browser walk-through |
| **Estimated runtime** | ~2 seconds (automated) + ~5 minutes (human walk-through) |

---

## Sampling Rate

- **After every task commit:** Run quick GID check command above
- **After every plan wave:** Run full suite (dev server + visual scan)
- **Before `/gsd:verify-work`:** All three done criteria must be satisfied
- **Max feedback latency:** 10 seconds for automated checks

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| Setup tileset registry | 01 | 1 | .tmx has all tilesets | manual | Open .tmx in Tiled, verify palette has all 12 tilesets | ❌ W0 | ⬜ pending |
| Retire generate-map.ts | 01 | 1 | RETIRED comment present | automated | `grep -c "RETIRED" src/game/maps/generate-map.ts` | ❌ W0 | ⬜ pending |
| Delete overworld-map.test.ts | 01 | 1 | test file removed | automated | `test ! -f src/game/maps/overworld-map.test.ts && echo "✓ deleted"` | ❌ W0 | ⬜ pending |
| Building facades — key buildings | 02 | 2 | No flat BUILDING_GID | automated | Quick run command above | ❌ W0 | ⬜ pending |
| Building facades — generic buildings | 02 | 2 | No flat BUILDING_GID | automated | Quick run command above | ❌ W0 | ⬜ pending |
| Ground/water visual pass | 03 | 3 | Meaningful visual improvement | manual | Walk-through + screenshot checklist | ❌ W0 | ⬜ pending |
| Boot.ts preload update | 04 | 4 | New tilesets load in browser | automated | `npm run dev` — no console errors | ❌ W0 | ⬜ pending |
| TMX committed to repo | 04 | 4 | .tmx file present | automated | `test -f public/assets/maps/overworld.tmx && echo "✓ present"` | ❌ W0 | ⬜ pending |
| GID chain integrity | 04 | 4 | firstgid values unchanged | automated | `node -e "const m=require('./public/assets/maps/overworld.json'); m.tilesets.slice(0,5).forEach(t=>console.log(t.name,t.firstgid))"` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — this phase has no test files to stub. All automated checks are inline Node.js one-liners that run against the exported overworld.json. The "Wave 0" for this phase is:

- [ ] Verify Tiled is installed and opens overworld.json without errors
- [ ] Confirm `npm run dev` serves the map before any edits (baseline green)
- [ ] Confirm quick GID check passes on the unmodified map (no false positives)

*Existing infrastructure covers all automated verification needs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Human walk-through approval | Done criterion 1 | Visual judgment — no automated equivalent | Run `npm run dev`, open browser, walk around entire map with WASD/arrows |
| Screenshot checklist | Done criterion 2 | Subjective visual quality | Screenshot: Main Street view, Thoven HQ facade, Andres's House, Beach strip, Water border — each must look intentional |
| Collision layer unchanged | Phase 4+ contract | Geometry is locked from 3.3 | Walk into buildings — collision should stop movement at the same tiles as before this phase |
| Layer names intact | Phaser integration | GameScene.ts references exact names | Open .tmx, verify layer names: "Ground", "Above", "Collision" (exact strings) |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or manual instructions
- [ ] Sampling continuity: automated GID check runs after every Tiled export
- [ ] Wave 0 baseline confirmed (map loads before edits)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
