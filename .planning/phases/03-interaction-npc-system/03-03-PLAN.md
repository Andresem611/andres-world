---
phase: 03-interaction-npc-system
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/game/ui/DialogBox.ts
autonomous: true
requirements: [INTER-04, INTER-05]

must_haves:
  truths:
    - "DialogBox is a Phaser.GameObjects.Container fixed to camera viewport (not world space)"
    - "Text appears instantly — no typewriter effect"
    - "Dialog pages show 2 lines per page; Space/E advances; advance() returns true on last page close"
    - "Movement cannot occur while dialog is open (isOpen() returns true)"
    - "splitIntoPages is a pure exported function importable by Vitest without Phaser"
  artifacts:
    - path: "src/game/ui/DialogBox.ts"
      provides: "DialogBox class + splitIntoPages pure function"
      exports: ["DialogBox", "splitIntoPages", "InteractionPayload"]
  key_links:
    - from: "src/game/ui/DialogBox.ts"
      to: "tests/dialog-box.test.ts"
      via: "export splitIntoPages"
      pattern: "export function splitIntoPages"
    - from: "src/game/ui/DialogBox.ts"
      to: "src/game/scenes/Overworld.ts"
      via: "DialogBox class used in Plan 04"
      pattern: "new DialogBox"
---

<objective>
Build the DialogBox UI component and define the InteractionPayload type. This plan runs in parallel with Plan 02 (no shared files). DialogBox is a camera-fixed Phaser Container that implements the Pokemon Gen 1/2 text box — white box, dark pixel border, monospace font, 2 lines per page, instant display, Space/E advance.

Purpose: The dialog and interaction payload contracts must exist before Plan 04 wires them into OverworldScene.
Output: src/game/ui/DialogBox.ts with DialogBox class + InteractionPayload type + pure splitIntoPages export.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-interaction-npc-system/03-CONTEXT.md
@.planning/phases/03-interaction-npc-system/03-RESEARCH.md
@.planning/phases/03-interaction-npc-system/03-01-SUMMARY.md

<interfaces>
<!-- Phaser 3.90.0 — Container + Text patterns (from installed type defs) -->
```typescript
// Camera-fixed container (must have setScrollFactor(0)):
const container = scene.add.container(0, 0);
container.setScrollFactor(0);  // CRITICAL — without this, box scrolls with camera
container.setDepth(100);       // above all map layers
container.setVisible(false);   // hidden by default

// Text object inside container:
const text = scene.add.text(x, y, "content", { fontFamily: "monospace", fontSize: "14px", color: "#000000" });
container.add(text);

// Rectangle for background:
const bg = scene.add.rectangle(x, y, width, height, 0xffffff);
bg.setStrokeStyle(3, 0x000000);
container.add(bg);
```

<!-- Game dimensions from main.ts -->
// width: 800, height: 600
// Dialog box target position: bottom 25% of screen
// Box dimensions: full width (800px wide), ~120px tall, y origin at ~470

<!-- Locked decisions from CONTEXT.md -->
// - Instant text display — NO typewriter effect
// - 2 lines per page
// - Space/E advances to next page, closes on last page
// - NO dim overlay — game stays visible and rendered behind box
// - White box, dark pixel border, pixel/monospace font
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DialogBox component + InteractionPayload type + splitIntoPages export</name>
  <files>src/game/ui/DialogBox.ts</files>
  <behavior>
    Pure function tests (Vitest):
    - splitIntoPages(["a","b","c","d"], 2) → [["a","b"],["c","d"]]
    - splitIntoPages(["a","b","c"], 2) → [["a","b"],["c"]]
    - splitIntoPages([], 2) → []
    - splitIntoPages(["a"], 2) → [["a"]]

    DialogBox class behavior (manual, cannot Vitest):
    - show(lines) populates pages via splitIntoPages(lines, 2), sets currentPage=0, renders page 0, makes container visible
    - advance() on page 0 of 2-page dialog increments page, renders page 1, returns false
    - advance() on last page hides container, returns true
    - isOpen() returns container.visible
    - Container has setScrollFactor(0) and setDepth(100)
    - Visual: white rectangle (#ffffff fill) with 3px dark border (#000000), monospace font 14px dark text, text padded 16px from box edges
  </behavior>
  <action>
    Create src/game/ui/DialogBox.ts with this exact structure:

    1. Export InteractionPayload union type (four variants: npc, sign, building, under_construction) — this type is the contract for the interaction router in Plan 04.

    2. Export pure function `splitIntoPages(lines: string[], linesPerPage: number): string[][]` — simple chunk function. Must be exported at module level (not a class method) so Vitest can import it without instantiating a Phaser class.

    3. Export DialogBox class:
      - Constructor takes `scene: Phaser.Scene`
      - Creates a Phaser.GameObjects.Container at (0, 0) with setScrollFactor(0) and setDepth(100)
      - Creates background rectangle: x=0, y=470, width=800, height=120, fill #ffffff, 3px #000000 stroke
      - Creates two Text objects (line1 at y=486, line2 at y=510) with monospace font 14px color #000000 — these are updated on each page render
      - All objects added to the container. Container setVisible(false) initially.
      - Private state: pages: string[][], currentPage: number

    Methods:
    - show(lines: string[]): void — calls splitIntoPages, sets currentPage=0, renders page, shows container
    - advance(): boolean — increments currentPage; if >= pages.length, hides container, returns true; else renders new page, returns false
    - isOpen(): boolean — returns this.container.visible
    - close(): void — hides container, resets state (for programmatic close without advance)
    - private renderPage(pageIndex: number): void — sets line1/line2 text from pages[pageIndex][0] and pages[pageIndex][1] (fallback to "" if undefined)

    ANTI-PATTERNS to avoid (from 03-RESEARCH.md):
    - Do NOT use setScrollFactor(1) or omit setScrollFactor — box will drift with camera
    - Do NOT position text using world coordinates — the container is in screen space
    - Do NOT implement typewriter effect — instant display only (user's locked decision)
  </action>
  <verify>
    <automated>npm test -- tests/dialog-box.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>dialog-box.test.ts passes all tests. splitIntoPages correctly chunks arrays. npm run build succeeds with no TypeScript errors in DialogBox.ts.</done>
</task>

</tasks>

<verification>
- `npm test -- tests/dialog-box.test.ts` all green
- `npm run build` no TypeScript errors in src/game/ui/DialogBox.ts
- InteractionPayload type exported and includes all four variants (npc, sign, building, under_construction)
</verification>

<success_criteria>
DialogBox.ts created with splitIntoPages export, InteractionPayload type, and DialogBox class implementing show/advance/isOpen/close. dialog-box.test.ts all green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-npc-system/03-03-SUMMARY.md`
</output>
