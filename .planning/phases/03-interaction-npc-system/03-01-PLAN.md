---
phase: 03-interaction-npc-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/npc-config.test.ts
  - tests/interaction-router.test.ts
  - tests/dialog-box.test.ts
autonomous: true
requirements: [INTER-01, INTER-02, INTER-03, INTER-04, INTER-05, NPC-01, NPC-02, NPC-03, NPC-04, NPC-05]

must_haves:
  truths:
    - "All 10 requirement behaviors have automated test coverage before implementation begins"
    - "Test suite runs in under 10 seconds with no Phaser/DOM dependencies"
    - "Tests are in failing (RED) state — no implementation exists yet"
  artifacts:
    - path: "tests/npc-config.test.ts"
      provides: "NPC config validation (NPC-01 through NPC-05)"
      exports: []
    - path: "tests/interaction-router.test.ts"
      provides: "Interaction dispatch logic (INTER-01, INTER-02, INTER-03)"
      exports: []
    - path: "tests/dialog-box.test.ts"
      provides: "Dialog pagination logic (INTER-04, INTER-05)"
      exports: []
  key_links:
    - from: "tests/npc-config.test.ts"
      to: "src/game/config/npcs.ts"
      via: "import NPC_CONFIG"
      pattern: "NPC_CONFIG"
    - from: "tests/interaction-router.test.ts"
      to: "src/game/ui/DialogBox.ts"
      via: "import InteractionPayload"
      pattern: "InteractionPayload"
    - from: "tests/dialog-box.test.ts"
      to: "src/game/ui/DialogBox.ts"
      via: "import DialogBox"
      pattern: "DialogBox"
---

<objective>
Create the three Wave 0 test files that validate all 10 Phase 3 requirements. These tests are written first (RED) and must fail until implementation is in place. All tests cover pure TypeScript logic — no Phaser, no DOM, no WebGL.

Purpose: Nyquist compliance requires test scaffolds exist before implementation tasks run. Every automated verify command in Plans 02-05 references these files.
Output: Three failing test files in tests/ that become green as implementation plans execute.
</objective>

<execution_context>
@/Users/andresmartinez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andresmartinez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interaction-npc-system/03-CONTEXT.md
@.planning/phases/03-interaction-npc-system/03-RESEARCH.md

<interfaces>
<!-- Existing test pattern — from tests/overworld-map.test.ts -->
```typescript
import { describe, it, expect } from "vitest";
// Tests import from project source — no Phaser, no DOM
```

<!-- Vitest config runs with: npm test -->
<!-- Test file naming convention: kebab-case in tests/ directory -->
<!-- All tests must be pure TypeScript (no WebGL/DOM) -->

<!-- Types the tests will validate against (to be created in Plan 02): -->
export interface NpcDefinition {
  id: string;
  name: string;
  tileX: number;
  tileY: number;
  spriteKey: string;
  facingDirection: Direction;
  dialog: string[];
  patrol?: false;
}

export interface PatrolNpcDefinition extends Omit<NpcDefinition, "patrol"> {
  patrol: true;
  patrolPath: Direction[];
}

export type InteractionPayload =
  | { type: "npc"; id: string; dialog: string[] }
  | { type: "sign"; text: string[] }
  | { type: "building"; key: string; returnPos: { x: number; y: number } }
  | { type: "under_construction"; message: string };
</interfaces>
</context>

<feature>
  <name>Wave 0 test scaffolds — all Phase 3 requirements</name>
  <files>tests/npc-config.test.ts, tests/interaction-router.test.ts, tests/dialog-box.test.ts</files>
  <behavior>
    npc-config.test.ts — NPC_CONFIG shape validation:
    - NPC-01: Every entry has a non-empty spriteKey string
    - NPC-02: Every entry has a dialog array with at least one string
    - NPC-03: No dialog string equals "TODO" or contains "[placeholder]"
    - NPC-04: Exactly one entry has patrol === true; that entry has patrolPath.length > 0; id is "john-collison"
    - NPC-05: NPC_CONFIG has exactly 14 entries (13 named + both dogs count as 2 separate entries)

    interaction-router.test.ts — InteractionPayload dispatch logic:
    - INTER-01: Given a Map<string, InteractionPayload> with key "5,10", looking up "5,10" returns the payload; looking up "5,11" returns undefined
    - INTER-02: A payload with type "building" has a key string and returnPos {x, y} with number values
    - INTER-03: A payload with type "under_construction" has a message string equal to "Builder still hammering away... check back soon."

    dialog-box.test.ts — Dialog pagination logic (pure functions, no Phaser):
    - INTER-04: splitIntoPages(["line1", "line2", "line3", "line4"], 2) returns [["line1","line2"], ["line3","line4"]]
    - INTER-05a: advance() on a 2-page dialog on page 0 returns false (more pages remain)
    - INTER-05b: advance() on a 1-page dialog returns true (dialog closes)
  </behavior>
  <implementation>
    Write tests that import from the files Plan 02 will create:
    - npc-config.test.ts imports NPC_CONFIG from "../../src/game/config/npcs" (will fail until Plan 02 creates it)
    - interaction-router.test.ts tests the InteractionPayload type shape inline (no import needed — tests pure object literals)
    - dialog-box.test.ts imports splitIntoPages from "../../src/game/ui/DialogBox" (will fail until Plan 03 creates it)

    All three test files must compile and run with `npm test` — some tests will fail (RED) because implementation doesn't exist yet, but the files must not have syntax errors.

    Use `vi.mock` sparingly — none needed here since all tested logic is pure TypeScript.

    For dialog-box.test.ts: since DialogBox wraps Phaser which can't run in Vitest, export `splitIntoPages` as a standalone pure function from DialogBox.ts that tests can import without instantiating the class. Test the function in isolation.
  </implementation>
</feature>

<verification>
<automated>npm test 2>&1 | grep -E "(FAIL|PASS|Tests)" | head -20</automated>
Test files exist and run (some will be RED — that is correct for Wave 0).
`npm test` must not crash with syntax errors. Missing module import errors are expected and acceptable at this stage.
</verification>

<success_criteria>
- tests/npc-config.test.ts exists with 5 describe blocks covering NPC-01 through NPC-05
- tests/interaction-router.test.ts exists with 3 test cases covering INTER-01, INTER-02, INTER-03
- tests/dialog-box.test.ts exists with 3 test cases covering INTER-04 and INTER-05
- `npm test` runs without crashing on syntax errors
- All tests targeting NPC_CONFIG fail with "Cannot find module" (correct RED state — implementation in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-interaction-npc-system/03-01-SUMMARY.md`
</output>
